# Shared

Cross-cutting code that can be imported by both the API and webapp. Keep shared schemas, validation utilities, constants, and other reusable TypeScript modules here to avoid duplication across services.
