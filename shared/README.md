# Shared (planned)

Cross-cutting code consumed by both the API and webapp layers.

Initial contents will include the current `shared/schema.ts`. Future work can introduce a `src/` subtree for utilities, types, and adapters shared across runtimes.
