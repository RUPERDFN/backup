# Landing (planned)

Public marketing site for CookFlow. Intended to host SEO-focused pages, legal documents, and static forms.

Planned contents:
- `public/` for assets such as `robots.txt`, `sitemap.xml`, `app-ads.txt`, and bridge scripts.
- Top-level legal pages like `privacy-policy.html` and `terms-of-service.html`.
